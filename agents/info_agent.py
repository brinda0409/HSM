from agents.hostel_information_agent import hostel_information_agent, HostelInformationAgent

# Backward-compatibility alias so existing imports of info_agent continue to work seamlessly
info_agent = hostel_information_agent
InfoAgent = HostelInformationAgent
